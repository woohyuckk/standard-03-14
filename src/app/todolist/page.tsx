interface Todos {
  id: string;
  title: string;
  contents: string;
  isCompleted: boolean;
  imgPath: string;
  createdAt: number;
}

const TodoListPage = () => {
  return <div>TodoListPage(CSR)</div>;
};

export default TodoListPage;
